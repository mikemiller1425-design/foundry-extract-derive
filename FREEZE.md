# FREEZE v0.1 — 2026-08-31

Frozen: words, tree, procedure, first derived SPEC (`biz_by_zip`).

Not frozen: whether BEE M1 actually passes. That is conductor’s job.

Do not add inbox automation or a second ticket until `biz_by_zip` has a decision sheet.
